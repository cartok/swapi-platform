version: 3
run: once
set: [pipefail]
output: prefixed

vars:
  DOCKER_IMAGE_NAME: swapi-angular

includes:
  client:
    taskfile: ./packages/client
    dir: ./packages/client
  server:
    taskfile: ./packages/server
    dir: ./packages/server
  shared:
    taskfile: ./packages/shared
    dir: ./packages/shared

tasks:
  clean:
    cmds:
      - task: client:clean
      - task: shared:clean
      - task: server:clean
      - rm -rf ./.cache || true

  install:
    aliases:
      - i
    preconditions:
      - sh: command -v bun
    cmds:
      - bun install --link-native-bins
      - bunx browser-driver-manager install chrome

  lint:
    vars:
      PROFILE:
        ref: .SWAPI_PROFILE | default .PROFILE | default "debug"
    requires:
      vars:
        - name: PROFILE
          enum: [debug, release]
    deps:
      - task: lint:sequence-collection
        vars:
          PROFILE: '{{.PROFILE}}'
      - task: lint:parallel-collection

  lint:parallel-collection:
    internal: true
    deps:
      - task: lint:fly

  lint:sequence-collection:
    internal: true
    vars:
      PROFILE:
        ref: .SWAPI_PROFILE | default .PROFILE | default "debug"
    requires:
      vars:
        - name: PROFILE
          enum: [debug, release]
    cmds:
      - task: lint:tsc
        vars:
          PROFILE: '{{.PROFILE}}'
      - task: lint:eslint
      - task: lint:prettier

  lint:fly:
    sources:
      - ./fly.template.yaml
    cmds:
      - flyctl config validate --config fly.template.yaml

  lint:tsc:
    vars:
      PROFILE:
        ref: .SWAPI_PROFILE | default .PROFILE | default "debug"
    requires:
      vars:
        - name: PROFILE
          enum: [debug, release]
    deps:
      - task: client:lint
        vars:
          PROFILE: '{{.PROFILE}}'
      - task: server:lint
        vars:
          PROFILE: '{{.PROFILE}}'
      - task: shared:lint
        vars:
          PROFILE: '{{.PROFILE}}'

  lint:eslint:
    sources:
      - ./eslint.config.mjs
      - ./package.json
      - ./packages/**/*.{ts,json}
    cmds:
      - bunx --no-install eslint ./

  lint:prettier:
    sources:
      - ./.prettierignore
      - ./package.json
      - ./prettier.config.mjs
      - ./packages/**/*.{ts,json}
    cmds:
      - bunx --no-install prettier --check ./

  fix:
    sources:
      - ./eslint.config.mjs
      - ./package.json
      - ./packages/**/*.{ts,json}
    cmds:
      - bunx --no-install eslint . --fix
      - task: format

  format:
    sources:
      - ./.prettierignore
      - ./package.json
      - ./prettier.config.mjs
      - ./packages/**/*.{ts,json}
    cmds:
      - bunx --no-install prettier --write --list-different ./

  docker:build:
    vars:
      PROFILE: &var-docker-profile
        ref: .SWAPI_PROFILE | default .PROFILE | default "release"
      TARGET: &var-docker-target
        ref: .SWAPI_TARGET | default .TARGET | default "local"
    requires:
      vars:
        - &requires-docker-profile
          name: PROFILE
          enum: [debug, release]
        - &requires-docker-target
          name: TARGET
          enum: [local, testing, production]
    dotenv:
      - &dotenv-docker-1 ./packages/server/.env/.env.{{.TARGET}}.{{.PROFILE}}.docker
      - &dotenv-docker-2 ./packages/server/.env/.env.{{.TARGET}}.{{.PROFILE}}
    cmds:
      - task: lint
      - >
        docker buildx build --load
        --build-arg NODE_ENV=${NODE_ENV}
        --build-arg PROFILE=${SWAPI_PROFILE}
        --build-arg TARGET=${SWAPI_TARGET}
        --tag {{.DOCKER_IMAGE_NAME}}:${SWAPI_TARGET}-${SWAPI_PROFILE}
        {{.CLI_ARGS}}
        ./

  docker:start:
    vars:
      PROFILE: *var-docker-profile
      TARGET: *var-docker-target
    requires:
      vars:
        - *requires-docker-profile
        - *requires-docker-target
    dotenv:
      - *dotenv-docker-1
      - *dotenv-docker-2
    deps:
      - task: docker:build
        vars:
          PROFILE: '{{.PROFILE}}'
          TARGET: '{{.TARGET}}'
    cmds:
      - |
        zsh -c '
          cleanup() {
            docker rm --force {{.DOCKER_IMAGE_NAME}} >/dev/null 2>&1 || true
          }
          trap cleanup EXIT INT TERM
          cleanup
          docker run --rm \
            --publish 127.0.0.1:${SWAPI_SERVER_PORT}:${SWAPI_SERVER_PORT} \
            --name {{.DOCKER_IMAGE_NAME}} \
            {{.DOCKER_IMAGE_NAME}}:{{.TARGET}}-{{.PROFILE}}
        '

  deploy:
    vars:
      PROFILE:
        ref: .SWAPI_PROFILE | default .PROFILE | default "release"
      TARGET:
        ref: .SWAPI_TARGET | default .TARGET | default "testing"
      CONFIG:
        sh: mktemp /tmp/fly.XXXXXX.yaml
    requires:
      vars:
        - name: PROFILE
          enum: [debug, release]
        - name: TARGET
          enum: [testing, production]
        - CONFIG
    dotenv:
      - ./.env.fly
      - *dotenv-docker-1
      - *dotenv-docker-2
    preconditions:
      - command -v envsubst
      - command -v shred
      - command -v openssl
      - command -v fly
      - test -s ./fly.template.yaml
      - test -s ./.env.fly
      - test -n "${SWAPI_SECRET_HEALTH_CHECK_TOKEN}"
      - printf '%s' "${SWAPI_SECRET_HEALTH_CHECK_TOKEN}" | openssl base64 -d -A >/dev/null 2>&1
    cmds:
      - task: lint
      - envsubst < ./fly.template.yaml > {{.CONFIG}}
      - defer: shred -u {{.CONFIG}}
      - >
        fly deploy
        --config {{.CONFIG}}
        --build-arg NODE_ENV=${NODE_ENV}
        --build-arg PROFILE=${SWAPI_PROFILE}
        --build-arg TARGET=${SWAPI_TARGET}
        --compression zstd
        --compression-level 7
        {{.CLI_ARGS}}

  generate-secret:
    silent: true
    preconditions:
      - command -v zsh
      - command -v openssl
      - command -v wl-copy
    cmd: |
      zsh -c '
        secret="$(openssl rand -base64 32)"
        wl-copy -- "${secret}" >/dev/null 2>&1
        result=$?
        if (( ${result} == 0 )); then
          print -- "A secret was generated and copied to your clipboard."
        else
          exit ${result}
        fi
      '
