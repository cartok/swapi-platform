# Docs Roadmap

- [ ] Write about PROFILE x TARGET build independency.
- [ ] Check repo init against clean environment.
  - [ ] Check if vscode taskfile extension resolves the installed taskfile dependency
- [ ] Create a better dependencies list. CLI tools like `envsubst` etc are not yet documented and the current format doesn't work well.
- [ ] Document secrets.
