# Tests Roadmap

For E2E see [e2e.md](./e2e.md)

- [ ] Non standard port for docker build tests
- [ ] Maybe add build matrix tests
  - [ ] Maybe add additional Docker targets for a full matrix: `bundled|unbundled x full-linux|distroless`.

---

- [ ] Make `server:dev` run directly from source by adding a Vite dev-server middleware (if feasible).

---

- [ ] Remove component unit tests & selectively create useful unit and integration tests
