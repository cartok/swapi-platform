# Worker Roadmap

- [ ] Should propably have both hono error handlers in sync (server, worker)
- [ ] Better target integration (right now fixed to use server:dev), also for later integration testing.
- [ ] Generally improve the worker taskfile
